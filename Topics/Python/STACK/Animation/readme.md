## Read Me

The folder contains files for Flask animation of stack operations. There aree three files:
- stk_v4.py: contains class file for basic stack operations
- app_stk.py: contains routes for Flask app invoked for displaying the stack and operations on it.
- index.html: contains the frontend function to control the visual display of operation with animation. 
